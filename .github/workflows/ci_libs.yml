name: CI_LIBS

on:
  push:
    paths:
      - 'libs/*'
    branches:
      - main
  pull_request:
    paths:
      - 'libs/*'
    branches:
      - main

jobs:
  lint:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v2
        with:
          version: v1.41.0
          working-directory: libs/

  test:
    runs-on: self-hosted
    needs: lint
    steps:
      - name: Install Go
        uses: actions/setup-go@v1
        with:
          go-version: 1.16

      - name: Checkout repository.
        uses: actions/checkout@v2

      - name: Start test.
        run: go test -race ./libs/...

